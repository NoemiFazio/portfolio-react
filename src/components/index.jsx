import "./index.css";

const Navbar = () => {
  return (
    <div className="navbar">
      <button>Home</button>
      <button>About</button>
      <button>Contacts</button>
    </div>
  );
};

export default Navbar;
